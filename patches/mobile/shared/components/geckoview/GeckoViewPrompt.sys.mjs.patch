diff --git a/mobile/shared/components/geckoview/GeckoViewPrompt.sys.mjs b/mobile/shared/components/geckoview/GeckoViewPrompt.sys.mjs
index 51ce0b4bd1b7..3d20d7af13c7 100644
--- a/mobile/shared/components/geckoview/GeckoViewPrompt.sys.mjs
+++ b/mobile/shared/components/geckoview/GeckoViewPrompt.sys.mjs
@@ -72,6 +72,11 @@ export class PromptFactory {
     }
 
     const type = target.type;
+    if (type === "color") {
+      this._handleColor(target);
+      aEvent.preventDefault();
+      return;
+    }
     if (type === "month" || type === "week") {
       // TODO(bug 888320, bug 1283382, bug 1283383): Remove this once we
       // support UA widgets for month/week inputs.
@@ -158,11 +163,22 @@ export class PromptFactory {
       mozSystemGroup: true,
     });
 
+    const elemRect = aElement.getBoundingClientRect();
+    const winUtils = aElement.ownerGlobal.windowUtils;
+    const screenRect = winUtils.toScreenRectInCSSUnits(
+      elemRect.left, elemRect.top, elemRect.width, elemRect.height
+    );
     prompt.asyncShowPrompt(
       {
         type: "choice",
         mode: aElement.multiple ? "multiple" : "single",
         choices: items,
+        rect: {
+          left: screenRect.left,
+          top: screenRect.top,
+          width: screenRect.width,
+          height: screenRect.height,
+        },
       },
       result => {
         deferredUpdate.disarm();
@@ -292,6 +308,50 @@ export class PromptFactory {
     );
   }
 
+  _handleColor(aElement) {
+    const win = aElement.ownerGlobal;
+    const elemRect = aElement.getBoundingClientRect();
+    const winUtils = win.windowUtils;
+    const screenRect = winUtils.toScreenRectInCSSUnits(
+      elemRect.left,
+      elemRect.top,
+      elemRect.width,
+      elemRect.height
+    );
+
+    const prompt = new lazy.GeckoViewPrompter(win);
+    aElement.addEventListener(
+      "blur",
+      () => prompt.dismiss(),
+      { mozSystemGroup: true, once: true }
+    );
+
+    prompt.asyncShowPrompt(
+      {
+        type: "color",
+        title: "",
+        value: aElement.value || "#000000",
+        predefinedValues: [],
+        rect: {
+          left: screenRect.left,
+          top: screenRect.top,
+          width: screenRect.width,
+          height: screenRect.height,
+        },
+      },
+      result => {
+        if (!result || !result.color) {
+          return;
+        }
+        if (result.color === aElement.value) {
+          return;
+        }
+        aElement.value = result.color;
+        this._dispatchEvents(aElement);
+      }
+    );
+  }
+
   _dispatchEvents(aElement) {
     // Fire both "input" and "change" events for <select> and <input> for
     // date/time.
