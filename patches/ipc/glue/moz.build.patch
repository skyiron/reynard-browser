diff --git a/ipc/glue/moz.build b/ipc/glue/moz.build
index a37300e74b03..639bfdbda6a5 100644
--- a/ipc/glue/moz.build
+++ b/ipc/glue/moz.build
@@ -150,10 +150,10 @@ if CONFIG["OS_ARCH"] != "WINNT":
 
 if CONFIG["TARGET_OS"] == "iOS":
     EXPORTS.mozilla.ipc += [
-        "ExtensionKitUtils.h",
+        "NSExtensionUtils.h",
     ]
     UNIFIED_SOURCES += [
-        "ExtensionKitUtils.mm",
+        "NSExtensionUtils.mm",
     ]
 
 EXPORTS.ipc += [
@@ -290,8 +290,8 @@ FINAL_LIBRARY = "xul"
 
 if CONFIG["TARGET_OS"] == "OSX":
     OS_LIBS += ["bsm"]  # for audit_token_to_pid
-elif CONFIG["TARGET_OS"] == "iOS":
-    OS_LIBS += ["-framework BrowserEngineKit"]
+# elif CONFIG["TARGET_OS"] == "iOS":
+    # OS_LIBS += ["-framework BrowserEngineKit"]
 elif CONFIG["MOZ_WIDGET_TOOLKIT"] == "android":
     OS_LIBS += ["android"]
 
